package map;

public class Info {

	/* Map is not chilInterface of collection
	 * if we want to represent  a group of object as KeyValuePairs then we should go for map
	 * Both ke and pairs are object only
	 * Duplicates key are not allowed.But values can be deplicates
	 * Each key-value pair is called entry
	 * hence a map is considers as a collection of entry object
	 * 
	 *  
	 * 
	 * 
	 * 
	 */
	
	
}
