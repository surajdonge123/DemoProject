package Sortedset;

public class Info {

	public static void main (String[]args) {
		/*It is child interface of set
		 * if we want to represent a group of individuals object  without duplicates
		  ans all object wil be inserted according to some sorting orders.
		 * *The sorting can be either default natural sorting or customized sorting orders 
		 * For String object default naural sorting is alphabetical order
		 * For number default natural sorting is ascending order
		 
		 *Methods===6
		 *objectfirst();return 1st element
		 *objectlast();return last element
		 *sortedSetheadSet(object obj); return sorted set whose elements are <Object
		 *sortedSettailSet(object obj); return Sorted whose elements are >=object
		 *sortedsetsubset(object obj1,object obj2); returns sorted set whpse elements are >=obj1 and <obj2
		 *comparator comparator();
		 */
		
		
	}
}
